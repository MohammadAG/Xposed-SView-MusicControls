<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="poweramp"
        android:entries="@array/pref_mediaplayer_list_titles"
        android:entryValues="@array/pref_mediaplayer_list_values"
        android:key="current_media_player"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_media_player" android:summary="@string/pref_description_media_player"/>
    <CheckBoxPreference android:summary="@string/pref_description_longpress" android:title="@string/pref_title_longpress_title" android:key="longpress_to_toggle_playback" android:defaultValue="false"/>
    <CheckBoxPreference android:summary="@string/disable_samsung_updates_description" android:key="disable_samsung_music_updates" android:title="@string/disable_samsung_updates_title"/>

</PreferenceScreen>